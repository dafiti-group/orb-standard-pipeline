description: |
  This example show how to use the grafana-notify in your application
  to annotate any chart
usage:
  version: 2.1
  orbs:
    dft: dafiti-group/orb-standard-pipeline@4.0.0
  workflows:
    deployment-flow:
      jobs:
        - dft/grafana-notify:
            context: [DEFAULT]
            target_env: live
