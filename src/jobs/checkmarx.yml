description: |
  This job will be the new checkmarx context.
  Need context:
    - CHECKMARX_CLOUD
executor: small
steps:
  - run:
      name: executing checkmarx
      command: echo "OK"
  - slack/notify:
      event: fail
      custom: <<include(templates/fail.json)>>
