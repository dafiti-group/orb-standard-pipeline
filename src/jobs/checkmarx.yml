description: This job will be the new checkmarx context.
executor: small
steps:
  - run:
      name: executing checkmarx
      command: echo "OK"
  - slack/notify:
      event: fail
      custom: <<include(templates/fail.json)>>
  # remove after testing
  - slack/notify:
      custom: <<include(templates/fail.json)>>
  - slack/notify:
      custom: <<include(templates/success.json)>>
  - slack/notify:
      custom: <<include(templates/hold.json)>>
